<main>
  <div class="shop-list">
    <ul>
      <% @shops.each do |shop| %>
      <li>
        <%= link_to shop_path(shop) do %>
        <div class="shop-list_detail">
          <div class="shop-list_image">
            <% if shop.top_banners.blank? %>
            <%= image_tag('no-image.png') %>
            <% else %>
            <%= attachment_image_tag @top_banner, :image %>
            <% end %>
          </div>
          <div class="shop-list_content">
            <h2><%= shop.name %></h2>
            <dl>
              <% if shop.access.blank? %>
              <% else %>
              <dt>アクセス：</dt>
              <dd><%= shop.access %></dd>
              <% end %>
            </dl>
            <dl>
              <dt>住所：</dt>
              <dd><%= shop.full_address %></dd>
            </dl>
            <dl>
              <% if shop.business_hours.blank? %>
              <% else %>
              <dt>営業時間：</dt>
              <dd><%= shop.business_hours %></dd>
              <% end %>
              <% if shop.holiday.blank? %>
              <% else %>
              <dt>定休日：</dt>
              <dd><%= shop.holiday %></dd>
              <% end %>
            </dl>
            <dl>
              <dt>レビュー：</dt>
              <dd>件</dd>
            </dl>
          </div>
        </div>
        <% end %>
      </li>
      <% end %>
    </ul>
  </div>
</main>
